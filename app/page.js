// Bu dosya kullanılmıyor — next.config.mjs beforeFiles rewrite ile / → /v3 yönlendiriliyor.
// LayoutShell da pathname === '/' durumunu v3 olarak tanıyor.
export default function Home() { return null; }
